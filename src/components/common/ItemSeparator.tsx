import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ItemSeparator = () => {
  return <View style={{height: 9}} />;
};

export default ItemSeparator;

const styles = StyleSheet.create({});
